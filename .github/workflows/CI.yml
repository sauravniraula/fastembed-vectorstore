# This file is autogenerated by maturin v1.10.2
# To update, run
#
#    maturin generate-ci github
#
name: CI

on:
  push:
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          # "zig" is highly recommended for cross-compiling to Linux ARM64
          # It handles glibc versioning automatically for manylinux compatibility
          zig: ${{ matrix.platform.target == 'aarch64' }}
          manylinux: auto
          # Add openssl/vendored feature to fix OpenSSL linking
          args: --release --out dist --features openssl/vendored
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          # We do NOT need to install apt packages for the host
          # because we are using 'vendored' openssl and zig/cross.
          before-script-linux: |
            if [ "${{ matrix.platform.target }}" = "x86_64" ]; then
                yum -y install openssl-devel perl-IPC-Cmd
            else
                apt update
                apt install -y libssl-dev pkg-config perl
            fi
        env:
          # 1. RUST FIX: Disable outline atomics in Rust code
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-Ctarget-feature=-outline-atomics"

          # 2. C FIX (Ring/OpenSSL):
          # -D__ARM_ARCH=8 fixes Ring assembly.
          # -mno-outline-atomics prevents GCC from generating the missing symbol in C code.
          CFLAGS_aarch64_unknown_linux_gnu: "-D__ARM_ARCH=8 -mno-outline-atomics"

          # 3. C++ FIX (ONNX Runtime):
          # This is the critical one for 'ort'. It prevents the C++ compiler
          # from linking against __aarch64_cas8_sync.
          CXXFLAGS_aarch64_unknown_linux_gnu: "-mno-outline-atomics"

      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          # "zig" is highly recommended for cross-compiling to Linux ARM64
          # It handles glibc versioning automatically for manylinux compatibility
          zig: ${{ matrix.platform.target == 'aarch64' }}
          manylinux: auto
          # Add openssl/vendored feature to fix OpenSSL linking
          args: --release --out dist --features openssl/vendored
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          # We do NOT need to install apt packages for the host
          # because we are using 'vendored' openssl and zig/cross.
          before-script-linux: |
            if [ "${{ matrix.platform.target }}" = "x86_64" ]; then
                yum -y install openssl-devel perl-IPC-Cmd
            else
                apt update
                apt install -y libssl-dev pkg-config perl
            fi
        env:
          # 1. RUST FIX: Disable outline atomics in Rust code
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-Ctarget-feature=-outline-atomics"

          # 2. C FIX (Ring/OpenSSL):
          # -D__ARM_ARCH=8 fixes Ring assembly.
          # -mno-outline-atomics prevents GCC from generating the missing symbol in C code.
          CFLAGS_aarch64_unknown_linux_gnu: "-D__ARM_ARCH=8 -mno-outline-atomics"

          # 3. C++ FIX (ONNX Runtime):
          # This is the critical one for 'ort'. It prevents the C++ compiler
          # from linking against __aarch64_cas8_sync.
          CXXFLAGS_aarch64_unknown_linux_gnu: "-mno-outline-atomics"

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist

  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          architecture: ${{ matrix.platform.target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - uses: actions/setup-python@v5
        with:
          python-version: 3.13t
          architecture: ${{ matrix.platform.target }}
      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist -i python3.13t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist

  macos:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-14
            target: x86_64
          - runner: macos-14
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist -i python3.13t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    needs: [linux, windows, macos, sdist]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to upload release artifacts
      contents: write
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/download-artifact@v4
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: "wheels-*/*"
      - name: Publish to PyPI
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing wheels-*/*
